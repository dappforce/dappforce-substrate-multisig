import { Generator$Calculation, Generator$Options } from './types';
export default function calculate(test: Array<string>, _address: string, { atOffset, withCase }: Generator$Options): Generator$Calculation;
